---
pagination:
  data: collections
  size: 1
  alias: tag
  filter:
    - nav
    - all
    - wants
    - posts
  addAllPagesToCollections: true
layout: layouts/base.njk
renderData:
  title: Wants Tagged “{{ tag | unslug | fixNames }}”
permalink: /wants/tagged/{{ tag }}/
---
{% from "macros.njk" import paginate %}

<h1>Tagged “{{ tag | unslug | fixNames }}”</h1>

{% set wants = collections[ tag ] %}
{% include "components/wants.njk" %}

{# {{ paginate( "Tag", pagination ) }} #}
